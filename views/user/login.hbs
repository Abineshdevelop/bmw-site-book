<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login - Dark Theme</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      background-color: #000;
      font-family: 'Segoe UI', sans-serif;
      color: #e0e0e0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .video-bg {
      position: fixed; inset: 0; z-index: -1; overflow: hidden;
    }
    .video-bg video {
      width: 100%; height: 100%; object-fit: cover; filter: brightness(0.65);
    }
    

    .login-card {
      background-color: rgba(0,0,0,0.35);
      backdrop-filter: blur(6px);
      border-radius: 15px;
      padding: 2.2rem;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
      max-width: 400px;
      width: 100%;
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(30px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .form-control {
      background-color: #1a1a1a;
      color: #fff;
      border: 1px solid #3a3a3a;
    }

    .form-control::placeholder {
      color: #bbb;
    }

    .form-control:focus {
      background-color: #222;
      border-color: #fff;
      box-shadow: 0 0 0 0.2rem rgba(255,255,255,0.25);
      color: #fff;
    }

    .btn-login {
      background: #fff;
      border: 1px solid #fff;
      height: 50px;
      border-radius: 10px;
      font-weight: bold;
      color: #000;
      transition: background 0.25s ease, color 0.25s ease, filter 0.25s ease;
    }

    .btn-login:hover { background: #000; color: #fff; filter: brightness(1.05); }

    .password-toggle {
      position: absolute;
      right: 1rem;
      top: 2.8rem;
      cursor: pointer;
      color: #ccc;
    }

    a { color: #fff; }
    a:hover { color: #fff; text-decoration: underline; }

    .text-muted {
      color: #ccc !important;
    }
  </style>
</head>

<body>
  <div class="video-bg">
    <video src="/images/InShot_20240924_102642383.mp4" autoplay muted loop playsinline></video>
  </div>
  <div class="login-card">
    <div class="text-center mb-3"><img src="https://upload.wikimedia.org/wikipedia/commons/4/44/BMW.svg" alt="BMW Logo" style="height:48px;"></div>
    <h2 class="text-center mb-3" style="color: #00bcd4;">Welcome Back</h2>
    <p class="text-center text-muted mb-4">Sign in to your account</p>

    <form id="loginForm" method="POST" action="/user/login" novalidate>

      <div class="mb-3">
        <label for="email" class="form-label">Email address</label>
        <input type="email" class="form-control" name="email" id="email" placeholder="Enter your email">
      </div>

      <div class="mb-3 position-relative">
        <label for="password" class="form-label">Password</label>
        <input type="password" name="password" class="form-control" id="password" placeholder="Enter your password">
        <i class="bi bi-eye password-toggle" onclick="togglePassword()"></i>
      </div>
      <div class="d-grid mb-3">
        <button type="submit" class="btn btn-login">Sign In</button>
      </div>

      <div class="text-center mt-3">
        <a href="/user/register">Register</a>
        <span style="color: #ccc;">New user </span>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <script>

    const message = '{{message}}';
    const type = '{{type}}';

    if (message && message.trim() !== '') {

  if (message && message.trim() !== '') {
    Swal.fire({
      icon: type || 'info',
      title: type === 'success' ? 'Success' :
             type === 'error' ? 'Error' :
             'Notice',
      text: message,
      background: "#1a1a1a",
      color: "#ffffff",
      iconColor: "#ffffff",
      confirmButtonColor: "#ffffff",
      confirmButtonText: 'OK',
      timer: type === 'success' ? 2000 : undefined,          
      timerProgressBar: type === 'success' ? true : false,  
      showConfirmButton: type === 'success' ? false : true 
    });
  }
    }


  </script>
  <script>
    document.getElementById('loginForm').addEventListener('submit', function (e) {
      e.preventDefault();


      const email = document.getElementById('email').value.trim();
      const password = document.getElementById('password').value.trim();

      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

      if (!email || !emailRegex.test(email)) {
        Swal.fire({
          icon: 'error',
          title: 'Invalid Email',
          text: 'Please enter a valid email address.',
          background: '#1f1f1f',
          color: '#e0e0e0',
          iconColor: '#00bcd4',
          confirmButtonColor: '#00bcd4'
        });
        return;
      }

      if (!password || password.length < 4) {
        Swal.fire({
          icon: 'error',
          title: 'Invalid Password',
          text: 'Password must be at least 4 characters long.',
          background: '#1f1f1f',
          color: '#e0e0e0',
          iconColor: '#00bcd4',
          confirmButtonColor: '#00bcd4'
        });
        return;
      }

      this.submit();
    });
  </script>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    function togglePassword() {
      const passwordInput = document.getElementById("password");
      const toggleIcon = document.querySelector(".password-toggle");
      const type = passwordInput.type === "password" ? "text" : "password";
      passwordInput.type = type;
      toggleIcon.classList.toggle("bi-eye");
      toggleIcon.classList.toggle("bi-eye-slash");
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <script>
    const urlParams = new URLSearchParams(window.location.search);
    const message = urlParams.get('message');
  </script>


</body>

</html>