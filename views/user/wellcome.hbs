<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BMW Virtual Dealership</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --bg: #0b0c10;
      --surface: #0f1116;
      --muted: #9aa4b2;
      --text: #e8ecf1;
      --primary: #1c69d4;
      --primary-700: #0a3d91;
      --accent: #14b8a6;
      --ring: rgba(28, 105, 212, 0.45);
    }

    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, 'Helvetica Neue', Arial, 'Noto Sans', 'Apple Color Emoji', 'Segoe UI Emoji'; color: var(--text); line-height: 1.6; background: radial-gradient(1200px 600px at 80% -10%, rgba(20,184,166,0.15), transparent 40%), radial-gradient(1000px 500px at 10% -20%, rgba(28,105,212,0.18), transparent 45%), var(--bg); overflow-x: hidden; }

    header { position: sticky; top: 0; z-index: 50; backdrop-filter: blur(8px); background: linear-gradient(180deg, rgba(15,17,22,0.9), rgba(15,17,22,0.6) 60%, transparent); border-bottom: 1px solid rgba(255,255,255,0.06); }
    .nav { max-width: 1200px; margin: 0 auto; padding: 16px 24px; display: flex; align-items: center; justify-content: space-between; }
    .brand { display: flex; align-items: center; gap: 12px; }
    .brand img { height: 36px; filter: drop-shadow(0 6px 14px rgba(28,105,212,0.45)); }
    .brand span { font-weight: 700; letter-spacing: 0.6px; }
    nav ul { list-style: none; display: flex; gap: 22px; }
    nav a { text-decoration: none; color: var(--text); font-weight: 600; font-size: 0.95rem; opacity: 0.9; transition: color .2s ease, opacity .2s ease; }
    nav a:hover { color: var(--primary); opacity: 1; }

    /* Hero */
    .hero { position: relative; min-height: 100vh; display: grid; place-items: center; text-align: center; padding: 88px 24px 48px; overflow: hidden; }
    .hero::before { content: ""; position: absolute; inset: -20%; background: radial-gradient(800px 400px at 50% 30%, rgba(28,105,212,0.25), transparent 60%), radial-gradient(800px 400px at 50% 70%, rgba(20,184,166,0.22), transparent 60%); filter: blur(20px); z-index: -2; }
    .bg-grid { position: absolute; inset: -10% -10% 0 -10%; background-image: linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px); background-size: 40px 40px; mask-image: radial-gradient(60% 60% at 50% 40%, black 30%, transparent 70%); animation: drift 16s linear infinite; z-index: -1; }
    @keyframes drift { from { transform: translateY(0); } to { transform: translateY(40px); } }

    .hero-inner { max-width: 980px; }
    .kicker { display: inline-flex; align-items: center; gap: 10px; padding: 8px 12px; border-radius: 999px; background: rgba(28,105,212,0.12); color: var(--primary); font-weight: 700; letter-spacing: 0.6px; backdrop-filter: blur(6px); border: 1px solid rgba(28,105,212,0.25); box-shadow: 0 0 0 6px var(--ring); animation: float 6s ease-in-out infinite; }
    @keyframes float { 0%,100%{ transform: translateY(0); } 50%{ transform: translateY(-4px); } }

    .hero h1 { font-size: clamp(2.2rem, 6vw, 4.25rem); line-height: 1.05; margin: 18px 0 12px; letter-spacing: -0.5px; text-shadow: 0 10px 24px rgba(0,0,0,0.45); }
    .subtitle { font-size: clamp(1.05rem, 2.2vw, 1.25rem); color: var(--muted); margin-bottom: 28px; }

    .cta-row { display: inline-flex; gap: 14px; flex-wrap: wrap; justify-content: center; }
    .btn { position: relative; padding: 12px 18px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.12); color: var(--text); background: linear-gradient(180deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02)); text-decoration: none; font-weight: 700; letter-spacing: .2px; transition: transform .15s ease, box-shadow .2s ease, border-color .2s ease; box-shadow: 0 6px 18px rgba(0,0,0,0.35), inset 0 0 24px rgba(255,255,255,0.02); }
    .btn:hover { transform: translateY(-2px); border-color: rgba(255,255,255,0.24); box-shadow: 0 10px 26px rgba(0,0,0,0.45), 0 0 0 6px var(--ring) inset; }
    .btn-primary { background: linear-gradient(180deg, rgba(28,105,212,0.95), rgba(10,61,145,0.95)); border-color: rgba(255,255,255,0.15); box-shadow: 0 10px 30px rgba(28,105,212,0.45); }
    .btn-primary:hover { box-shadow: 0 16px 40px rgba(28,105,212,0.6); }
    .btn-ghost { background: linear-gradient(180deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02)); }

    .badge-pulse { position: absolute; inset: -2px; border-radius: 14px; border: 2px solid rgba(20,184,166,0.45); filter: blur(3px); opacity: 0; animation: pulse 2s ease-in-out infinite; pointer-events: none; }
    .btn-primary:hover .badge-pulse { opacity: 1; }
    @keyframes pulse { 0%{ opacity: 0; transform: scale(.98); } 50%{ opacity: 1; transform: scale(1.02); } 100%{ opacity: 0; transform: scale(1.04); } }

    .showcase { position: relative; margin-top: 28px; display: grid; place-items: center; }
    .car { width: min(86vw, 980px); border-radius: 16px; overflow: hidden; box-shadow: 0 24px 80px rgba(0,0,0,0.55); transform: translateY(20px) scale(0.98); opacity: 0; transition: transform .8s cubic-bezier(.2,.8,.2,1), opacity .8s ease; }
    .car.reveal { transform: translateY(0) scale(1); opacity: 1; }
    .car img { width: 100%; height: auto; display: block; }

    /* Sections */
    section { scroll-margin-top: 80px; }
    .section { padding: 80px 20px; }
    .container { max-width: 1200px; margin: 0 auto; }
    .section h2 { font-size: clamp(1.6rem, 3vw, 2.2rem); margin-bottom: 16px; }
    .section p.lead { color: var(--muted); max-width: 760px; }

    .features { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 18px; margin-top: 28px; }
    .card { background: linear-gradient(180deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02)); border: 1px solid rgba(255,255,255,0.08); border-radius: 14px; padding: 22px; transition: transform .18s ease, border-color .2s ease, box-shadow .2s ease; box-shadow: 0 10px 28px rgba(0,0,0,0.25) inset; opacity: 0; transform: translateY(18px); }
    .card:hover { transform: translateY(-4px); border-color: rgba(255,255,255,0.18); box-shadow: 0 18px 40px rgba(0,0,0,0.35) inset; }
    .card.reveal { opacity: 1; transform: translateY(0); }
    .card h3 { margin-bottom: 6px; }
    .card p { color: var(--muted); }

    footer { border-top: 1px solid rgba(255,255,255,0.06); background: rgba(15,17,22,0.6); text-align: center; padding: 26px 18px; color: var(--muted); }

    /* Admin CTA Fab */
    .fab { position: fixed; right: 20px; bottom: 20px; z-index: 60; }
    .fab a { display: inline-flex; align-items: center; gap: 8px; padding: 12px 16px; border-radius: 999px; background: linear-gradient(180deg, rgba(28,105,212,0.95), rgba(10,61,145,0.95)); color: white; text-decoration: none; border: 1px solid rgba(255,255,255,0.16); box-shadow: 0 14px 36px rgba(28,105,212,0.45); font-weight: 700; letter-spacing: 0.2px; transition: transform .15s ease, box-shadow .2s ease; }
    .fab a:hover { transform: translateY(-2px); box-shadow: 0 20px 46px rgba(28,105,212,0.6); }

    /* Responsive */
    @media (max-width: 640px) {
      .nav { padding: 14px 16px; }
      nav ul { gap: 14px; }
      .btn { width: 100%; justify-content: center; }
      .cta-row { width: 100%; }
    }
  </style>
</head>
<body>
  <header>
    <div class="nav">
      <div class="brand">
        <img src="/images/BMW-logo.png" alt="BMW Logo">
        <span>BMW Virtual Dealership</span>
      </div>
      <nav>
        <ul>
          <li><a href="#">Home</a></li>
          <li><a href="#about">About</a></li>
          <li><a href="#features">Features</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <section class="hero">
    <div class="bg-grid"></div>
    <div class="hero-inner">
      <span class="kicker">Sheer Driving Pleasure</span>
      <h1>Experience the future of BMW, virtually.</h1>
      <p class="subtitle">Explore, configure, and book your next BMW with immersive visuals and seamless interactions.</p>
      <div class="cta-row">
        <a href="/user/login" class="btn btn-primary">
          Login to Continue
          <span class="badge-pulse"></span>
        </a>
        <a href="#features" class="btn btn-ghost">See what’s inside</a>
      </div>
      <div class="showcase">
        <div class="car" id="carReveal">
          <img src="/images/BMW-X5.jpg" alt="BMW X5">
        </div>
      </div>
    </div>
  </section>

  <section class="section" id="about">
    <div class="container">
      <h2>About</h2>
      <p class="lead">Our virtual dealership brings the BMW experience to your screen. Compare models, discover features, and schedule a test drive — all with cinematic visuals and effortless flow.</p>
    </div>
  </section>

  <section class="section" id="features">
    <div class="container">
      <h2>Features</h2>
      <div class="features">
        <div class="card"><h3>🚗 Explore Models</h3><p>View the latest lineup with stunning imagery.</p></div>
        <div class="card"><h3>🧭 Smart Configurator</h3><p>Tailor trims and options that match your drive.</p></div>
        <div class="card"><h3>📅 Book Test Drive</h3><p>Reserve a slot at your convenience in seconds.</p></div>
        <div class="card"><h3>🛠️ Virtual Assistance</h3><p>Get instant answers from our AI concierge.</p></div>
      </div>
    </div>
  </section>

  <footer id="contact">
    <p>📞 Contact: +91 80158 62881 | ✉️ Email: bmwvirtualdealership@gmail.com</p>
    <p>© 2025 BMW Virtual Dealership</p>
  </footer>

  <div class="fab">
    <a href="/admin/login" aria-label="Admin Login">🔐 Admin Login</a>
  </div>

  <script>
    // Reveal effects on scroll
    const onIntersect = (entries, obs) => {
      entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('reveal'); obs.unobserve(e.target); } });
    };
    const observer = new IntersectionObserver(onIntersect, { threshold: 0.2 });
    document.querySelectorAll('.card, #carReveal').forEach(el => observer.observe(el));
  </script>
</body>
</html>